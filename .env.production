# Production environment
NEXT_PUBLIC_APP_NAME=kai-umi

# Database (Docker compose internal DNS)
DATABASE_URL=postgresql://app:app_pw@db:5432/fish_kai
POSTGRES_USER=app
POSTGRES_PASSWORD=app_pw
POSTGRES_DB=fish_kai

# SMTP (replace with your provider)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_password
